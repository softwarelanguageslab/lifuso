package utils

/** Class that defines the main data structure to compare APIs given their vector representation.
 * @param vectors The double vectors of the API usages.
 */

case class API_Usage_Vector(vectors: Array[Array[Double]])
