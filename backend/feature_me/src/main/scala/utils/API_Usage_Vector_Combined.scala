package utils

/** Class that represent the combinations of the selected vectors into a single vector.
 * @param combinedVector The combined vector mixed from the others
 */

case class API_Usage_Vector_Combined(combinedVector: Array[Double] = Array())
